# cheese_erp environment config
# Auto-generated by setup-server.sh — placed at /opt/erpnext/.env

# Image tag: demo (staging) or latest (production)
TAG=demo

# Environment label: staging or production
DEPLOY_ENV=staging

# Traefik routing rule (frappe_docker compose.https.yaml pattern)
SITES_RULE=Host(`erp-cheese.deepzide.com`)

# Let's Encrypt certificate email
LETSENCRYPT_EMAIL=lesterdprez.work@gmail.com

# Domain for this environment
DOMAIN=erp-cheese.deepzide.com
